init_args:
  _target_: models.generators.llm.LLM
  model_name: "/path/to/pa-rag-qwen/dpo_cq"  # local checkpoint path — replace with your own trained PA-RAG (Qwen) DPO model
  max_new_tokens: 128
  max_length: 2048
  batch_size: 8
  use_attr: false
  use_cad: false
  no_context: false
